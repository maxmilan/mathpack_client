$(function () {
  new Highcharts.Chart({
    chart: { renderTo: 'function_plot' },
    title: { text: 'sin(x)' },
    xAxis: { tickInterval: 0.5 },
    yAxis: {
      title: { text: 'y'}
    },
    series: [{
      data: <%= data %>,
      pointStart: 0
    }]
  });
});
